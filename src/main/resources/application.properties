spring.application.name=condominium

# Perfil padrão ativo (Local)
spring.profiles.active=local

# ============================================
# JWT (Secret key e expiração de 7 dias)
# ============================================
jwt.secret=${JWT_SECRET:***REMOVED***}
jwt.expiration=604800000

# ============================================
# Swagger / Documentation
# ============================================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ============================================
# Configuração de E-mail (Gmail)
# ============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:***REMOVED***}
spring.mail.password=${MAIL_PASSWORD:***REMOVED***}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
